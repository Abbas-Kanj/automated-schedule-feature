// Generates dynamic font classes (`font-inter`, etc.) — adding a font here
// also needs a Google Fonts <link> in index.html and a matching
// `--font-*` variable in index.css.
export const fonts = ['inter', 'manrope', 'system'] as const
